name: Disconnected Readiness Summary

on:
  schedule:
    - cron: '17 * * * *'
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      repo:
        description: "Single repo to score (e.g. odh-dashboard). Leave empty to score all."
        required: false
        type: string
        default: ""
      parallel:
        description: "Number of repos to score concurrently"
        required: false
        type: string
        default: "4"

jobs:
  summary:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0

      - name: Install dependencies
        run: |
          uv sync --extra report --frozen
          make install-arch-analyzer

      - name: Run batch scorer
        env:
          INPUT_REPO: ${{ inputs.repo }}
          INPUT_PARALLEL: ${{ inputs.parallel || '4' }}
        run: |
          set -euo pipefail
          args=(--parallel "$INPUT_PARALLEL" --verbose)
          if [ -n "$INPUT_REPO" ]; then
            args+=(--repo "$INPUT_REPO")
          fi
          uv run run_all.py "${args[@]}" || true

      - name: Show summary
        if: always()
        run: |
          summary=$(find reports -name summary.md -type f 2>/dev/null | head -1)
          if [ -n "$summary" ]; then
            cat "$summary" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "⚠️ No summary.md generated" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: readiness-reports
          path: reports/
          retention-days: 30

      - name: Generate maturity report
        if: always()
        run: |
          set -euo pipefail
          report_dir=$(find reports -mindepth 1 -maxdepth 1 -type d | sort | tail -1)
          if [ -n "$report_dir" ]; then
            uv run python maturity_report.py "$report_dir" \
              --output disconnected-readiness-report.json \
              --run-url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
              --version "$(git describe --tags --always 2>/dev/null || echo 'unknown')"
          fi

      - name: Upload maturity report
        if: always() && hashFiles('disconnected-readiness-report.json') != ''
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: disconnected-readiness-report
          path: disconnected-readiness-report.json
          retention-days: 10
